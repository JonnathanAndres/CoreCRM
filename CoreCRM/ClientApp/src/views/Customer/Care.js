import React from 'react';
import { connect } from 'dva';
// import styles from './Index.css';

function Care() {
  return (
    <div>Customer/Care</div>
  );
}

Care.propTypes = {
};

export default connect()(Care);
